from django.apps import AppConfig


class RetrievalConfig(AppConfig):
    """Configure the retrieval application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.retrieval"
