from django.apps import AppConfig


class DocumentsConfig(AppConfig):
    """Configure the documents application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.documents"
