[project]
name = "documind-rag"
version = "0.1.0"
description = "Production-grade, evaluated RAG backend built with Django REST Framework."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "Django>=5.2,<5.3",
    "djangorestframework>=3.15,<3.16",
    "psycopg[binary]>=3.2,<3.3",
    "pgvector>=0.3,<0.4",
    "redis>=5.2,<6.0",
    "gunicorn>=23.0,<24.0",
    "numpy>=1.26,<2.3",
    "sentence-transformers>=3.3,<4.0",
    "anthropic>=0.40,<1.0",
    "openai>=1.55,<2.0",
]

[dependency-groups]
dev = [
    "djangorestframework-stubs>=3.15,<3.16",
    "django-stubs[compatible-mypy]>=5.1,<5.2",
    "mypy>=1.13,<1.14",
    "pytest>=8.3,<8.4",
    "pytest-django>=4.9,<4.10",
    "ruff>=0.8,<0.9",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.dev"
python_files = ["test_*.py", "*_test.py"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["*/migrations/*.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.11"
plugins = ["mypy_django_plugin.main"]
check_untyped_defs = true
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
exclude = ["^\\.venv/", "^data/", "^apps/.*/migrations/"]

[[tool.mypy.overrides]]
module = [
    "redis",
    "pgvector.*",
    "sentence_transformers.*",
    "transformers.*",
    "torch.*",
    "huggingface_hub.*",
    "anthropic.*",
    "openai.*",
]
ignore_missing_imports = true
follow_imports = "skip"

[tool.django-stubs]
django_settings_module = "config.settings.dev"

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
